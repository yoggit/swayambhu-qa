# zero-to-green — Environment Variables
# Copy this file to .env and fill in your values
# .env is gitignored — never commit real credentials

# ─── GitHub (default source) ──────────────────────────────────────────────────
# Already handled by gh CLI auth (gh auth login) — no extra vars needed

# ─── JIRA / Atlassian ─────────────────────────────────────────────────────────
# Usage: /qa-pipeline QA-42 --source jira
#
# Get your API token: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_BASE_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=your.email@company.com
JIRA_API_TOKEN=your_jira_api_token_here

# ─── Azure DevOps ─────────────────────────────────────────────────────────────
# Usage: /qa-pipeline 12345 --source ado
#
# Create PAT: dev.azure.com → User Settings → Personal Access Tokens
# Required scope: Work Items (Read)
ADO_ORG=yourorganization
ADO_PROJECT=YourProjectName
ADO_PAT=your_ado_personal_access_token_here

# ─── Linear ───────────────────────────────────────────────────────────────────
# Usage: /qa-pipeline ENG-456 --source linear
#
# Create API key: linear.app → Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_your_key_here

# ─── Optional overrides ───────────────────────────────────────────────────────
# BASE_URL=http://localhost:3000   # Override Playwright baseURL for local testing
